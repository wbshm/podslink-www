
  # App landing page design

  This is a code bundle for App landing page design. The original project is available at https://www.figma.com/design/yifSo7mYeEf1V2DFhbUQrP/App-landing-page-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  